// WAP to insert int numbers into List<int> and display in terminal.

import 'dart:io';

void main() {
  List<int> numbers = [];

  numbers.add(10);
  numbers.add(5);
  numbers.add(3);

  print(numbers);

  // Second Metod

  // stdout.write("Enter the Size of an Array = ");
  // int n = int.parse(stdin.readLineSync()!);

  // List num = [];

  // for (int i = 0; i < n; i++) {
  //   stdout.write("Enter the ${i + 1} Array Number = ");
  //   num.add(int.parse(stdin.readLineSync()!));
  // }

  // print(num);
}
